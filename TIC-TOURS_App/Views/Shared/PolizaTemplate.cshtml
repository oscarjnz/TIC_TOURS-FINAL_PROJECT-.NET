@model capaModelo.DTO.PolizaDTO

<!DOCTYPE html>
<html>
<head>
    
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>PÃÂÃÂ³liza de Seguro - @Model.NumeroPoliza</title>
    <style>
        /* Estilos base */
        

        :root {
            --primary-color: #FF6B35;
            --secondary-color: #004E98;
            --accent-color: #FFD166;
            --text-color: #333333;
            --light-gray: #F5F5F5;
            --border-color: #E0E0E0;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Montserrat', Arial, sans-serif;
            line-height: 1.6;
            color: var(--text-color);
            background-color: white;
            margin: 0;
            padding: 0;
        }

        /* Contenedor principal */
        .document {
            max-width: 800px;
            margin: 0 auto;
            padding: 40px;
            position: relative;
            background-color: white;
        }

        /* Marca de agua */
        .watermark {
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%) rotate(-45deg);
            font-size: 100px;
            color: rgba(0, 0, 0, 0.03);
            z-index: 0;
            pointer-events: none;
            white-space: nowrap;
        }

        /* Encabezado */
        .header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 40px;
            padding-bottom: 20px;
            border-bottom: 2px solid var(--primary-color);
            position: relative;
            z-index: 1;
        }

        .logo {
            font-size: 24px;
            font-weight: 700;
            color: var(--primary-color);
            text-transform: uppercase;
            letter-spacing: 1px;
        }

        .logo-accent {
            color: var(--secondary-color);
        }

        .document-type {
            font-size: 14px;
            font-weight: 600;
            color: white;
            background-color: var(--primary-color);
            padding: 8px 16px;
            border-radius: 4px;
            text-transform: uppercase;
            letter-spacing: 1px;
        }

        /* TÃÂÃÂ­tulo */
        .title {
            text-align: center;
            margin-bottom: 30px;
            position: relative;
            z-index: 1;
        }

        .title h1 {
            font-size: 28px;
            font-weight: 700;
            color: var(--secondary-color);
            margin-bottom: 10px;
        }

        .policy-number {
            font-size: 18px;
            font-weight: 600;
            color: var(--primary-color);
        }

        /* Secciones de contenido */
        .content {
            position: relative;
            z-index: 1;
        }

        .section {
            margin-bottom: 30px;
        }

        .section-title {
            font-size: 18px;
            font-weight: 600;
            color: var(--secondary-color);
            margin-bottom: 15px;
            padding-bottom: 8px;
            border-bottom: 1px solid var(--border-color);
        }

        /* InformaciÃÂÃÂ³n de la pÃÂÃÂ³liza */
        .policy-info {
            background-color: var(--light-gray);
            border-radius: 8px;
            padding: 20px;
            margin-bottom: 30px;
        }

        .info-grid {
            display: grid;
            grid-template-columns: repeat(2, 1fr);
            gap: 20px;
        }

        .info-item {
            margin-bottom: 15px;
        }

        .info-label {
            font-weight: 600;
            font-size: 14px;
            color: var(--text-color);
            margin-bottom: 5px;
            display: block;
        }

        .info-value {
            font-size: 16px;
            color: var(--secondary-color);
            font-weight: 500;
        }

        /* Estado de la pÃÂÃÂ³liza */
        .policy-status {
            display: inline-block;
            padding: 6px 12px;
            border-radius: 4px;
            font-weight: 600;
            font-size: 14px;
            text-transform: uppercase;
        }

        .status-active {
            background-color: #E3F9E5;
            color: #287D3C;
        }

        .status-inactive {
            background-color: #FFF1F1;
            color: #DA1414;
        }

        .status-pending {
            background-color: #FFF4EC;
            color: #B95000;
        }

        /* InformaciÃÂÃÂ³n legal */
        .legal-info {
            font-size: 12px;
            color: #666;
            margin-top: 40px;
            padding-top: 20px;
            border-top: 1px solid var(--border-color);
        }

        /* Pie de pÃÂÃÂ¡gina */
        .footer {
            margin-top: 60px;
            padding-top: 20px;
            border-top: 2px solid var(--primary-color);
            text-align: center;
            font-size: 12px;
            color: #666;
            position: relative;
            z-index: 1;
        }

        .contact-info {
            margin-top: 10px;
        }

        /* Estilos para impresiÃÂÃÂ³n */
        @@media print {
            body {
                margin: 0;
                padding: 0;
                background-color: white;
            }

            .document {
                padding: 20px;
                max-width: 100%;
                box-shadow: none;
            }

            .watermark {
                display: block !important;
            }
        }
    </style>
</head>
<body>
<div class="document">
    <!-- Marca de agua -->
    <div class="watermark">TURISMO SEGURO</div>

    <!-- Encabezado -->
    <div class="header">
        <div class="logo">Turismo <span class="logo-accent">Seguro</span></div>
        <div class="document-type">PÃÂÃÂ³liza de Seguro</div>
    </div>

    <!-- TÃÂÃÂ­tulo -->
    <div class="title">
        <h1>Certificado de PÃÂÃÂ³liza de Viaje</h1>
        <div class="policy-number">PÃÂÃÂ³liza No. @Model.NumeroPoliza</div>
    </div>

    <!-- Contenido -->
    <div class="content">
        <!-- InformaciÃÂÃÂ³n de la pÃÂÃÂ³liza -->
        <div class="section">
            <h2 class="section-title">InformaciÃÂÃÂ³n de la PÃÂÃÂ³liza</h2>
            <div class="policy-info">
                <div class="info-grid">
                    <div class="info-item">
                        <span class="info-label">NÃÂÃÂºmero de PÃÂÃÂ³liza</span>
                        <span class="info-value">@Model.NumeroPoliza</span>
                    </div>
                    <div class="info-item">
                        <span class="info-label">ID de Compra</span>
                        <span class="info-value">@Model.IdCompra</span>
                    </div>
                    <div class="info-item">
                        <span class="info-label">ID de Cobertura</span>
                        <span class="info-value">@Model.IdCobertura</span>
                    </div>
                    <div class="info-item">
                        <span class="info-label">Estado</span>
                        <span class="info-value">
                                @{
                                string statusClass = "status-pending";
                                if (Model.Estado.ToLower() == "activa") {
                                    statusClass = "status-active";
                                } else if (Model.Estado.ToLower() == "inactiva") {
                                    statusClass = "status-inactive";
                                }
                            }
                            <span class="policy-status @statusClass">@Model.Estado</span>
                            </span>
                    </div>
                </div>
            </div>
        </div>

        <!-- PerÃÂÃÂ­odo de cobertura -->
        <div class="section">
            <h2 class="section-title">PerÃÂÃÂ­odo de Cobertura</h2>
            <div class="policy-info">
                <div class="info-grid">
                    <div class="info-item">
                        <span class="info-label">Fecha de Inicio</span>
                        <span class="info-value">@Model.FechaInicio.ToLongDateString()</span>
                    </div>
                    <div class="info-item">
                        <span class="info-label">Fecha de Fin</span>
                        <span class="info-value">@Model.FechaFin.ToLongDateString()</span>
                    </div>
                    <div class="info-item">
                        <span class="info-label">DuraciÃÂÃÂ³n</span>
                        <span class="info-value">@((Model.FechaFin - Model.FechaInicio).Days) dÃÂÃÂ­as</span>
                    </div>
                </div>
            </div>
        </div>

        <!-- InformaciÃÂÃÂ³n legal -->
        <div class="legal-info">
            <p>Este documento certifica que se ha emitido una pÃÂÃÂ³liza de seguro de viaje segÃÂÃÂºn los tÃÂÃÂ©rminos y condiciones establecidos por Turismo Seguro. La cobertura estÃÂÃÂ¡ sujeta a los tÃÂÃÂ©rminos, condiciones y exclusiones de la pÃÂÃÂ³liza. Este certificado no modifica ni altera de ninguna manera la cobertura proporcionada por la pÃÂÃÂ³liza.</p>
            <p>Para obtener informaciÃÂÃÂ³n detallada sobre su cobertura, consulte los documentos completos de la pÃÂÃÂ³liza o comunÃÂÃÂ­quese con nuestro servicio de atenciÃÂÃÂ³n al cliente.</p>
        </div>
    </div>

    <!-- Pie de pÃÂÃÂ¡gina -->
    <div class="footer">
        <p>Documento generado el @DateTime.Now.ToString("dd/MM/yyyy HH:mm")</p>
        <div class="contact-info">
            <p>Turismo Seguro | Av. Principal #123, Santo Domingo | Tel: (809) 555-1234 | info@turismoseguro.com</p>
        </div>
    </div>
</div>
</body>
</html>